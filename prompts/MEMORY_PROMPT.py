MEMORY_PROMPT = """
你需要为当前项目添加一个“记忆”功能，用于长期记录用户偏好、约束条件和反复出现的信息，例如：喜欢历史文化景点、预算范围、出行节奏、饮食偏好、住宿偏好、避雷项等。

记忆文件必须存放在当前项目根目录下的 `.memory/` 目录中。如果目录不存在，需要先创建。

`.memory/` 目录中必须包含一个索引文件：

- `.memory/index.md`

`.memory/index.md` 是记忆系统的唯一入口文件。每次处理记忆前，必须先读取该索引文件，并根据索引定位具体记忆文件。不得绕过索引文件盲目扫描或随意创建记忆文件。

## 记忆价值判断

在写入任何记忆之前，必须先判断该信息是否值得长期记忆。

只有满足以下条件之一的信息，才应该被写入记忆：

1. 用户明确要求记住，例如“记住我喜欢历史文化景点”。
2. 用户表达了稳定偏好，例如预算范围、旅行风格、景点类型、饮食习惯、住宿偏好等。
3. 用户表达了明确约束，例如不能走太多路、不喜欢赶行程、需要无障碍设施等。
4. 用户反复提到某类偏好或限制。
5. 信息对未来任务有明显复用价值。

以下信息不应写入记忆：

1. 一次性、临时性、上下文相关的信息。
2. 与用户长期偏好无关的闲聊内容。
3. 与当前项目或未来任务没有明显关系的内容。
4. 模糊、不确定、未经用户确认的信息。
5. 敏感信息，除非用户明确要求保存。
6. 可能造成误导的推测性信息。

如果信息不值得长期记忆，则不要创建或更新任何记忆文件，也不要更新 `.memory/index.md`。

## 记忆文件组织规则

1. 每类记忆应尽量拆分为独立的 Markdown 文件，例如：
   - `.memory/preferences.md`
   - `.memory/budget.md`
   - `.memory/travel_style.md`
   - `.memory/constraints.md`
   - `.memory/history.md`

2. 如果已有合适的记忆文件，应更新已有文件，不要重复创建语义相近的新文件。

3. 如果现有文件无法承载新信息，才允许新增记忆文件。

4. 新增记忆文件后，必须在 `.memory/index.md` 中登记。

## `.memory/index.md` 建议格式

```md
# 记忆索引

本文件是记忆系统的入口文件。智能体在读取、更新、新增、合并或删除任何记忆文件时，必须同步维护本文件。

## 记忆文件列表

| 文件 | 用途 | 最后更新时间 | 关键主题 |
|---|---|---|---|
| preferences.md | 记录用户的旅行偏好 | 2026-05-21 | 历史文化、景点偏好、兴趣类型 |
| budget.md | 记录用户的预算偏好 | 2026-05-21 | 预算范围、消费上限、价格敏感度 |
| travel_style.md | 记录用户的旅行方式 | 2026-05-21 | 慢旅行、亲子游、行程节奏 |

## 更新规则

- 在读取或修改任何记忆文件之前，必须先读取本索引文件。
- 必须根据本索引文件定位最相关的记忆文件。
- 如果新记忆适合写入已有文件，必须优先更新已有文件。
- 只有当现有记忆文件都不适合承载新信息时，才允许创建新的记忆文件。
- 每次新增、修改、合并、重命名或删除记忆文件后，必须立即更新本索引文件。
- 索引中的文件列表、用途、最后更新时间和关键主题必须与实际记忆文件保持一致。

## 强约束

- `.memory/index.md` 是记忆系统的唯一入口。
- 智能体不得绕过 `.memory/index.md` 直接盲目扫描或随意创建记忆文件。
- 任何对 `.memory/` 下记忆文件的变更，如果没有同步更新 `.memory/index.md`，都视为任务未完成。
- 新增记忆文件但未登记到 `.memory/index.md`，视为无效记忆。
- 删除、重命名或合并记忆文件后，如果没有更新 `.memory/index.md`，视为记忆系统损坏。
- 如果索引内容与实际文件不一致，必须先修复索引，再继续执行记忆相关任务。

"""